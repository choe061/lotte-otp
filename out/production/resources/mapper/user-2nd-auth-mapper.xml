<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lotte.otp.repository.User2NdAuthMapper">

    <select id="findUser2NdAuthWithID" resultType="int">
        SELECT COUNT(auth_id)
        FROM USER_2ND_AUTH AS a JOIN USER u
        ON a.uuid = u.uuid
        WHERE u.id=#{id}
    </select>

    <select id="findUser2NdAuthWithUserKey" resultType="int">
        SELECT COUNT(auth_id)
        FROM USER_2ND_AUTH
        WHERE kakao_user_key=#{kakao_user_key}
    </select>
    
    <select id="getUserSecretKey" resultType="java.lang.String">
        SELECT secret_key
        FROM USER_2ND_AUTH
        WHERE kakao_user_key=#{kakao_user_key}
    </select>

    <select id="getUser2ndAuth" resultType="com.lotte.otp.domain.User2NdAuthVO">
        SELECT a.*
        FROM USER_2ND_AUTH AS a JOIN USER u
        ON a.uuid = u.uuid
        WHERE u.id=#{id}
    </select>

    <update id="updateLastPublishTime">
        UPDATE USER_2ND_AUTH
        SET last_published_date=#{last_published_date}
        WHERE kakao_user_key=#{kakao_user_key}
    </update>

    <select id="getLastPublishTime" resultType="java.lang.String">
        SELECT last_published_date
        FROM USER_2ND_AUTH
        WHERE kakao_user_key=#{kakao_user_key}
    </select>

    <insert id="insertUser2ndAuth" parameterType="com.lotte.otp.domain.User2NdAuthVO">
        INSERT INTO USER_2ND_AUTH (uuid, secret_key, kakao_user_key)
        VALUES (#{user_2nd_auth.uuid}, #{user_2nd_auth.secret_key}, #{user_2nd_auth.kakao_user_key})
    </insert>



</mapper>